# Этап 4: Ступень 1 «Младший специалист Внешнего Мира» — Барака — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Вторая ступень карьеры: два события в переговорке «Кровавый колодец» (квартальный обзор KPI и конфликт в чате) и код-ревью-бой с тимлидом Баракой, с реальным фоном переговорки, портретами и спрайтами. Появляется флаг `baraka_respect`, который используют следующие ступени.

**Architecture:** Контент ступени — один файл `src/content/ranks/01-junior.ts`, подключаемый в `src/content/index.ts` после `RANK_INTERN`. Ассеты ступени — записи группы `rank1` в `assets/manifest.json`, генерируются пайплайном этапа 2. Движок не меняется: ни `state`, ни `battle`, ни экраны, ни загрузчик — этап полностью «данные + картинки».

**Tech Stack:** тот же, что на этапах 1–3. Генерация: `npm run gen -- --group rank1`.

**Spec:** `docs/superpowers/specs/2026-09-03-corporate-mortal-kombat-design.md`

## Global Constraints

- Ступень не последняя: ровно 2 события, 2–4 варианта в каждом, первый вариант без `requiresFlag`, каждый `requiresFlag` кем-то ставится (`intern_joker` ставится на ступени 0, `baraka_respect` — в первом событии этой ступени).
- Барака: терпение 110, атака 11, слабость `agree`, иммунитет `joke`, `final: false`. Текст второго события содержит намёк на слабость (`/соглас|лоял|кива/i`) и иммунитет (`/юмор|шут|смеш/i`) — это проверяет тест `test/content.test.ts` из этапа 3.
- Эффекты варианта — в пределах ±15 по стату, стресс ±5..10.
- Фон ступени один: `bg_meeting_room` (1600×900, 2K). Портреты 512×512, спрайты 700×900, маджента `#FF00FF`, босс смотрит **влево** (герой вправо).
- В описании Бараки нет ни розового, ни маджентового, ни фиолетового: палитра — серый, бурый, оливковый, сталь. Иначе части персонажа уйдут в прозрачность при хромакее.
- Порядок генерации: портрет neutral → idle → attack/hurt/defeated (референсы: стиль + портрет + idle) и портрет angry (референс: neutral).
- Все реплики и тексты на русском, тон — офисная сатира без грубости.
- Барака обращается к игроку на «ты» (Милина — на «вы»): это характеризация, фиксируем осознанно.
- Dev-контент не трогаем: `dev0` удалён на этапе 3, финальный `dev1` с `dev_shao` остаётся до этапа 8.
- Коммит после каждой задачи.

---

### Task 1: Контент ступени 1

**Files:**
- Create: `src/content/ranks/01-junior.ts`
- Modify: `src/content/index.ts`, `assets/manifest.json` (id ассетов должны существовать — добавляются в Task 2, поэтому **Task 1 и Task 2 коммитятся одним коммитом**; до Task 2 тест манифеста и валидатор красные — это ожидаемо).
- Без изменений: `src/content/ranks/dev.ts`, `test/schema.test.ts`, `test/content.test.ts` — реальный контент должен проходить существующие тесты как есть.

**Interfaces:**
- Produces: `export const RANK_JUNIOR: Rank` с `id: 'junior'`, `background: 'bg_meeting_room'`, событиями `junior_kpi`, `junior_chat_war`, боссом `baraka`. Ассеты: `bg_meeting_room`, `pt_baraka_neutral`, `pt_baraka_angry`, `sp_baraka_idle`, `sp_baraka_attack`, `sp_baraka_hurt`, `sp_baraka_defeated`.
- Флаги: ставит `baraka_respect` (первое событие, первый вариант); читает `intern_joker` (ступень 0) и `baraka_respect` (второе событие).

- [ ] **Step 1: Написать `src/content/ranks/01-junior.ts`**

```ts
import type { Rank } from '../../types';

export const RANK_JUNIOR: Rank = {
  id: 'junior',
  title: 'Младший специалист Внешнего Мира',
  background: 'bg_meeting_room',
  events: [
    {
      id: 'junior_kpi',
      title: 'KPI',
      speaker: { name: 'Барака', portrait: 'pt_baraka_neutral' },
      text: 'Переговорка «Кровавый колодец», квартальный обзор KPI. На слайде красным: скорость закрытия задач — сорок семь процентов. Сорок. Семь. Мои линейки скрежещут по столу, и это не нервное, это метрика. Кто-нибудь объяснит, почему спринт закрыт, а Земное Царство — нет? Молчите. Колодец в полу тоже молчит, а он вообще-то для брейнштормов.',
      repeatText: 'Снова обзор KPI, снова сорок семь процентов на слайде. Я делаю вид, что не помню, как ты мне это уже объяснял. Линейки скрежещут чуть громче. Колодец по-прежнему молчит.',
      choices: [
        {
          text: 'Признать: цифры честные. Предложить план на следующий спринт',
          effects: { competence: 10, loyalty: 5, stress: 5 },
          setFlag: 'baraka_respect',
          reaction: { portrait: 'pt_baraka_neutral', text: 'Хм. Не оправдываешься. Редкость. Запиши план в трекер до конца дня — и я, может быть, перестану скрежетать.' },
        },
        {
          text: 'Метрика неправильная: половина задач — дубликаты',
          effects: { competence: 10, reputation: 5, stress: 10 },
          reaction: { portrait: 'pt_baraka_angry', text: 'ДУБЛИКАТЫ?! Я лично заводил каждую! …Ладно. Покажешь на ретро. Но линейки я не убираю.' },
        },
        {
          text: 'Кивать и записывать всё, что говорит Барака',
          effects: { loyalty: 10, competence: -5, stress: -5 },
          reaction: { portrait: 'pt_baraka_neutral', text: 'Вот. Вот так. Смотрите все: человек кивает. Учитесь.' },
        },
      ],
    },
    {
      id: 'junior_chat_war',
      title: 'Конфликт в чате',
      speaker: { name: 'Барака', portrait: 'pt_baraka_neutral' },
      text: 'В чате команды двести четырнадцать непрочитанных, половина — мои. Рептилия из поддержки написал, что моё код-ревью «идёт дольше, чем захват Земного Царства». Я ответил. Семнадцать раз. Заглавными. Теперь по делу: завтра твой первый пул-реквест на ревью ко мне. Совет тимлида: я ценю тех, кто согласен с замечаниями с первого раза. И я не понимаю шуток. Совсем. Не пытайся.',
      repeatText: 'Чат снова горит, я снова пишу капсом. Напоминаю: правильный ответ на ревью — «согласен», а не шутка. В прошлый раз ты перепутал.',
      choices: [
        {
          text: 'Написать в чат: «Предлагаю обсудить голосом»',
          effects: { reputation: 5, loyalty: 5, stress: 5 },
          reaction: { portrait: 'pt_baraka_neutral', text: 'Голосом? Хорошо. Я буду говорить громко. Это ты предложил.' },
        },
        {
          text: 'Молча поставить лайк сообщению Бараки',
          effects: { loyalty: 10, reputation: -5, stress: -5 },
          reaction: { portrait: 'pt_baraka_neutral', text: 'Я видел твой лайк. Я запомнил твой лайк.' },
        },
        {
          text: 'Пошутить в чате про «ревью длиннее спринта»',
          requiresFlag: 'intern_joker',
          effects: { reputation: 10, loyalty: -10, stress: 10 },
          reaction: { portrait: 'pt_baraka_angry', text: 'ЭТО. НЕ. СМЕШНО. И я не понимаю, почему Китана поставила смайлик.' },
        },
        {
          text: 'Предложить Бараке чеклист ревью, чтобы спорить было не о чём',
          requiresFlag: 'baraka_respect',
          effects: { competence: 10, loyalty: 5, stress: -5 },
          reaction: { portrait: 'pt_baraka_neutral', text: 'Чеклист. Двадцать пунктов. Первый пункт: «согласиться с Баракой». Принято.' },
        },
      ],
    },
  ],
  boss: {
    id: 'baraka',
    name: 'Барака',
    title: 'Тимлид',
    patience: 110,
    attack: 11,
    weakness: 'agree',
    immunity: 'joke',
    final: false,
    sprites: {
      idle: 'sp_baraka_idle', attack: 'sp_baraka_attack', hurt: 'sp_baraka_hurt', defeated: 'sp_baraka_defeated',
    },
    portraits: { neutral: 'pt_baraka_neutral', angry: 'pt_baraka_angry' },
    intro: 'Открываю твой пул-реквест. Сорок семь файлов. Начнём с первой строки. У меня есть время до конца квартала.',
    lines: {
      // hit звучит и как реакция на удар героя, и как реплика Бараки в его собственной атаке —
      // фразы должны работать в обе стороны
      hit: [
        'Так. Дальше по диффу.',
        'Допустим. Следующий файл.',
        'Отметил. Вернёмся к этому на ретро.',
        'Линейки говорят, что отступ поехал. Я пока молчу.',
      ],
      immune: [
        'Смешно? Я не понял. Комментарий «исправить» остаётся.',
        'В пул-реквесте не шутят. В пул-реквесте страдают.',
        'Ха. Ха. Это был не смех, это я считал отступы.',
      ],
      special: [
        'REQUEST CHANGES! ВЕЗДЕ ТАБЫ ВМЕСТО ПРОБЕЛОВ!',
        'Переписать всё. С нуля. На языке Внешнего Мира.',
      ],
      defeated: [
        'Approve. Не смотри на меня так. Мержи, пока я не передумал. И убери из кода слово «синергия».',
      ],
    },
  },
};
```

Второе событие — последнее перед боем, поэтому именно его проверяет тест намёков: «согласен» (в `text` и `repeatText`) → слабость `agree` по `/соглас|лоял|кива/i`; «шуток» / «шутка» / «СМЕШНО» → иммунитет `joke` по `/юмор|шут|смеш/i`. Формы «соглашается» / «соглашаются» под регэксп **не** подходят (в них «соглаш», а не «соглас») — поэтому в тексте именно «согласен».

- [ ] **Step 2: Подключить в `src/content/index.ts`**

```ts
import { RANK_INTERN } from './ranks/00-intern';
import { RANK_JUNIOR } from './ranks/01-junior';
import { DEV_RANKS } from './ranks/dev';

export const RANKS: Rank[] = [RANK_INTERN, RANK_JUNIOR, ...DEV_RANKS];
```

`src/content/ranks/dev.ts` не менять: в нём остаётся только финальный `dev1` (`dev_shao`, `final: true`), он по-прежнему последний в массиве, валидатор доволен. Dev-записей ступени 1 в манифесте нет — удалять нечего.

- [ ] **Step 3: Убедиться, что существующие тесты покрывают новую ступень**

Ничего не добавлять. `test/content.test.ts` (этап 3) пробегает по всем `CONTENT.ranks` с событиями и проверяет намёки по `HINT_WORDS` — `junior` попадает в проверку автоматически. `test/schema.test.ts` проверяет реальный контент валидатором: 2 события, 2–4 варианта, первый без `requiresFlag`, `intern_joker` и `baraka_respect` кем-то ставятся, `weakness ≠ immunity`, `final: false` у не-последней ступени. `test/content.test.ts` не менять.

- [ ] **Step 4: Не коммитить до Task 2** (иначе тест манифеста и валидатор красные — id `bg_meeting_room`, `pt_baraka_*`, `sp_baraka_*` ещё не в манифесте).

---

### Task 2: Ассеты ступени 1 в манифесте

**Files:**
- Modify: `assets/manifest.json`

**Interfaces:**
- Produces: 7 записей группы `rank1` с промптами. Описание персонажа для промптов:

```
BARAKA: a huge hulking Tarkatan man, Team Lead at Outworld Corp. Grey-brown leathery skin, bald head with bony ridges running back over the skull, tiny fierce deep-set eyes, and an enormous mouth full of rows of long curved fangs that never fully closes. Grey business shirt with the sleeves rolled up over massive forearms, a loosened dark charcoal tie, a corporate lanyard with a badge, dark olive cargo trousers with many pockets and black work boots. Instead of arm blades he wears two long steel ruler-like blades strapped to his forearms with leather straps, engraved with measurement marks, extending well past his hands. Intimidating, impatient, oddly professional. Palette strictly greys, browns, dark olive and matte, non-reflective brushed steel — absolutely no pink, no magenta, no purple anywhere on the character.
```

Портрет angry: «…roaring with his mouth wide open, every row of long fangs fully visible» — узнаваемая фишка персонажа. Линейки вместо клинков — шутка про тимлида, который «меряет отступы»; в бою в руке ноутбук с красным экраном ошибки. Красный на экране допустим: он далеко от мадженты в YCbCr и хромакей его не трогает.

**Блок BARAKA вставляется в каждый промпт целиком.** Генератор ничего не подставляет: в `manifest.json` не должно остаться текста `<описание>`.

- [ ] **Step 1: Фон**

```json
{
  "id": "bg_meeting_room", "kind": "background", "group": "rank1", "size": [1600, 900], "resolution": "2K",
  "file": "img/bg/meeting_room.webp", "model": "bytedance-seed/seedream-5-0-pro", "generated": false,
  "references": ["assets/reference/post-hero-and-shao-kahn.png"],
  "prompt": "Wide establishing shot of a corporate meeting room of Outworld Corp, the room the staff call the Blood Well: a long dark conference table with black office chairs, a big wall screen showing a red bar chart, a whiteboard covered with sticky notes and a flowchart, a wall poster with the single word OUTWORLD. In the centre of the floor sits a round dark stone well like a pit, lit from within by a deep red glow, with a small blank laminated sign standing on its rim — the room's brainstorm well. Skull trophies on a sideboard, a horned skull mounted above the screen, an iron dragon emblem on the wall. Empty of people. Wide-angle, eye level, deep depth of field, clean composition with the lower third free for UI. Exception to the no-text rule: the single word OUTWORLD on the wall poster is required; no other text anywhere."
}
```

- [ ] **Step 2: Портреты**

```json
{
  "id": "pt_baraka_neutral", "kind": "portrait", "group": "rank1", "size": [512, 512], "character": "baraka",
  "file": "img/portraits/baraka_neutral.webp", "model": "bytedance-seed/seedream-5-0-pro", "generated": false,
  "references": ["assets/reference/post-hero-and-shao-kahn.png"],
  "prompt": "Head-and-shoulders portrait, looking directly at camera with an impatient evaluating stare, tiny eyes narrowed, mouth slightly open showing rows of fangs, one steel ruler-blade visible at the edge of the frame. BARAKA: a huge hulking Tarkatan man, Team Lead at Outworld Corp. Grey-brown leathery skin, bald head with bony ridges running back over the skull, tiny fierce deep-set eyes, and an enormous mouth full of rows of long curved fangs that never fully closes. Grey business shirt with the sleeves rolled up over massive forearms, a loosened dark charcoal tie, a corporate lanyard with a badge, dark olive cargo trousers with many pockets and black work boots. Instead of arm blades he wears two long steel ruler-like blades strapped to his forearms with leather straps, engraved with measurement marks, extending well past his hands. Intimidating, impatient, oddly professional. Palette strictly greys, browns, dark olive and matte, non-reflective brushed steel — absolutely no pink, no magenta, no purple anywhere on the character. Background: blurred meeting room with a whiteboard covered in sticky notes and a horned skull on the wall."
},
{
  "id": "pt_baraka_angry", "kind": "portrait", "group": "rank1", "size": [512, 512], "character": "baraka",
  "file": "img/portraits/baraka_angry.webp", "model": "bytedance-seed/seedream-5-0-pro", "generated": false,
  "references": ["assets/reference/post-hero-and-shao-kahn.png", "pt_baraka_neutral"], "dependsOn": ["pt_baraka_neutral"],
  "prompt": "Same man, same framing and background as the reference portrait. BARAKA roaring with his mouth wide open, every row of long curved fangs fully visible, eyes bulging, brow ridges pulled down, veins standing out on the neck, loosened tie swinging. BARAKA: a huge hulking Tarkatan man, Team Lead at Outworld Corp. Grey-brown leathery skin, bald head with bony ridges running back over the skull, tiny fierce deep-set eyes, and an enormous mouth full of rows of long curved fangs that never fully closes. Grey business shirt with the sleeves rolled up over massive forearms, a loosened dark charcoal tie, a corporate lanyard with a badge, dark olive cargo trousers with many pockets and black work boots. Instead of arm blades he wears two long steel ruler-like blades strapped to his forearms with leather straps, engraved with measurement marks, extending well past his hands. Intimidating, impatient, oddly professional. Palette strictly greys, browns, dark olive and matte, non-reflective brushed steel — absolutely no pink, no magenta, no purple anywhere on the character."
}
```

- [ ] **Step 3: Спрайты** (`character: "baraka"`, `size: [700, 900]`, `chroma: "#FF00FF"`, `flip: false`, `file: img/sprites/baraka_<pose>.webp`)

| id | dependsOn | references | prompt |
|----|-----------|------------|--------|
| `sp_baraka_idle` | `pt_baraka_neutral` | `assets/reference/post-hero-and-shao-kahn.png`, `pt_baraka_neutral` | «Full body, head to boots fully visible, BARAKA standing in a wide impatient stance, three-quarter view facing LEFT, holding an open laptop in one hand at chest height with its screen showing a solid red error page, the other arm hanging with the steel ruler-blade pointing down, mouth open showing fangs, both steel ruler-blades fully inside the frame, nothing cropped at the edges. BARAKA: a huge hulking Tarkatan man, Team Lead at Outworld Corp. Grey-brown leathery skin, bald head with bony ridges running back over the skull, tiny fierce deep-set eyes, and an enormous mouth full of rows of long curved fangs that never fully closes. Grey business shirt with the sleeves rolled up over massive forearms, a loosened dark charcoal tie, a corporate lanyard with a badge, dark olive cargo trousers with many pockets and black work boots. Instead of arm blades he wears two long steel ruler-like blades strapped to his forearms with leather straps, engraved with measurement marks, extending well past his hands. Intimidating, impatient, oddly professional. Palette strictly greys, browns, dark olive and matte, non-reflective brushed steel — absolutely no pink, no magenta, no purple anywhere on the character. Isolated on a flat uniform solid magenta background (#FF00FF), no floor, no shadow on the background, even studio lighting from the right.» |
| `sp_baraka_attack` | `pt_baraka_neutral`, `sp_baraka_idle` | `assets/reference/post-hero-and-shao-kahn.png`, `pt_baraka_neutral`, `sp_baraka_idle` | «Same man as the reference sprite, still facing LEFT at the same three-quarter angle. BARAKA lunging forward to the LEFT, thrusting the red-screen laptop out with one hand and slashing downward with the ruler-blade on the other arm, jaws wide, roaring, both steel ruler-blades fully inside the frame, nothing cropped at the edges. BARAKA: a huge hulking Tarkatan man, Team Lead at Outworld Corp. Grey-brown leathery skin, bald head with bony ridges running back over the skull, tiny fierce deep-set eyes, and an enormous mouth full of rows of long curved fangs that never fully closes. Grey business shirt with the sleeves rolled up over massive forearms, a loosened dark charcoal tie, a corporate lanyard with a badge, dark olive cargo trousers with many pockets and black work boots. Instead of arm blades he wears two long steel ruler-like blades strapped to his forearms with leather straps, engraved with measurement marks, extending well past his hands. Intimidating, impatient, oddly professional. Palette strictly greys, browns, dark olive and matte, non-reflective brushed steel — absolutely no pink, no magenta, no purple anywhere on the character. Same magenta background, same framing scale.» |
| `sp_baraka_hurt` | `pt_baraka_neutral`, `sp_baraka_idle` | `assets/reference/post-hero-and-shao-kahn.png`, `pt_baraka_neutral`, `sp_baraka_idle` | «Same man, still facing LEFT at the same three-quarter angle. BARAKA recoiling backward (his back moves to the RIGHT), laptop tilting in his hand, the other ruler-blade raised across his face in a blocking gesture, tie flying, fangs bared in surprise. BARAKA: a huge hulking Tarkatan man, Team Lead at Outworld Corp. Grey-brown leathery skin, bald head with bony ridges running back over the skull, tiny fierce deep-set eyes, and an enormous mouth full of rows of long curved fangs that never fully closes. Grey business shirt with the sleeves rolled up over massive forearms, a loosened dark charcoal tie, a corporate lanyard with a badge, dark olive cargo trousers with many pockets and black work boots. Instead of arm blades he wears two long steel ruler-like blades strapped to his forearms with leather straps, engraved with measurement marks, extending well past his hands. Intimidating, impatient, oddly professional. Palette strictly greys, browns, dark olive and matte, non-reflective brushed steel — absolutely no pink, no magenta, no purple anywhere on the character. Same magenta background, same framing scale.» |
| `sp_baraka_defeated` | `pt_baraka_neutral`, `sp_baraka_idle` | `assets/reference/post-hero-and-shao-kahn.png`, `pt_baraka_neutral`, `sp_baraka_idle` | «Same man, still facing LEFT at the same three-quarter angle. BARAKA sitting slumped on the floor, legs stretched out, both ruler-blades resting limp on the ground, closed laptop on his knees, head bowed, mouth closed for once, a small green sticky note stuck to his forehead. BARAKA: a huge hulking Tarkatan man, Team Lead at Outworld Corp. Grey-brown leathery skin, bald head with bony ridges running back over the skull, tiny fierce deep-set eyes, and an enormous mouth full of rows of long curved fangs that never fully closes. Grey business shirt with the sleeves rolled up over massive forearms, a loosened dark charcoal tie, a corporate lanyard with a badge, dark olive cargo trousers with many pockets and black work boots. Instead of arm blades he wears two long steel ruler-like blades strapped to his forearms with leather straps, engraved with measurement marks, extending well past his hands. Intimidating, impatient, oddly professional. Palette strictly greys, browns, dark olive and matte, non-reflective brushed steel — absolutely no pink, no magenta, no purple anywhere on the character. Same magenta background, same framing scale.» |

В JSON это `"references": ["assets/reference/post-hero-and-shao-kahn.png", "pt_baraka_neutral", "sp_baraka_idle"]` и `"dependsOn": ["pt_baraka_neutral", "sp_baraka_idle"]` у attack/hurt/defeated, `"dependsOn": ["pt_baraka_neutral"]` у idle: каждый референс-по-id обязан быть и в `dependsOn`, иначе генератор может взять ещё не сгенерированную картинку.

Поза `defeated` ниже остальных — групповой кроп прижимает все позы к одной линии низа, это нормально: персонаж «осядет» на землю. Барака заметно крупнее Милины, но холст тот же 700×900: постобработка масштабирует по общему bounding box четырёх поз, поэтому в бою он будет выглядеть массивнее только за счёт пропорций, не размера спрайта.

- [ ] **Step 4: Тесты и коммит (вместе с Task 1)**

Run: `npm run typecheck && npm test && npm run gen -- --dry-run --group rank1`
Expected: тесты PASS (реальный контент проходит валидатор, id из контента есть в манифесте, намёки на `agree` и `joke` найдены во втором событии, `baraka_respect` ставится), dry-run показывает 7 записей на ≈ 0.41 $ (0.09 фон + 6 × 0.045 + 15 референсов × 0.003). Музыкальных записей в группе `rank1` нет — в списке ровно 7.

```bash
git add -A && git commit -m "feat(rank1): junior events, Baraka boss and asset manifest"
```

---

### Task 3: Генерация ассетов ступени 1

**Files:**
- Modify: `assets/manifest.json` (`generated`, `anchor`), `public/assets/img/**`

- [ ] **Step 1: Фон и портрет**

Run: `npm run gen -- --only bg_meeting_room,pt_baraka_neutral`
Критерии для фона: переговорка, длинный стол, экран с красным графиком, круглый колодец в полу с красным свечением, черепа как декор, пустая, без текста кроме OUTWORLD, нижняя треть свободна. Для портрета: серо-бурая кожа, гребни на лысой голове, клыки видны при полуоткрытом рте, рубашка с закатанными рукавами и ослабленный галстук, ничего розового или фиолетового. Брак → правка промпта, `--force`.

- [ ] **Step 2: Остальное**

Run: `npm run gen -- --group rank1`
Затем `npm run assets:check`. Открыть `public/assets/img/sprites/baraka_*.webp`: маджента вырезана без розовой каймы, линейки-клинки целые (сталь не ушла в прозрачность), красный экран ноутбука на месте, все позы одного масштаба, смотрят влево. Портрет angry — пасть раскрыта, все ряды клыков видны. Если модель нарисовала клинки как оригинальные костяные лезвия, а не линейки — усилить в промпте «flat steel rulers with engraved centimetre marks, NOT organic blades» и перегенерировать только спрайты и портрет neutral: `--only pt_baraka_neutral,sp_baraka_idle,sp_baraka_attack,sp_baraka_hurt,sp_baraka_defeated --force` (angry зависит от neutral, поэтому его тоже: добавить `pt_baraka_angry` в `--only`). Если Барака на спрайте смотрит вправо — не перегенерировать, а поставить `flip: true` и перекатать кроп по сырым PNG.

- [ ] **Step 3: Тесты**

Run: `npm test`
Expected: тест бюджета PASS (спрайты ≤ 150 КБ, портреты ≤ 80 КБ, фон ≤ 300 КБ, сумма ≤ 11.5 МБ). Если спрайт больше — перезапустить постобработку по сырым PNG с меньшим качеством (а не пережимать готовый WebP):

```bash
python3 tools/postprocess.py character --out-dir public/assets/img/sprites --size 700x900 --chroma FF00FF --quality 72 \
  sp_baraka_idle=assets/raw/sp_baraka_idle.png sp_baraka_attack=assets/raw/sp_baraka_attack.png \
  sp_baraka_hurt=assets/raw/sp_baraka_hurt.png sp_baraka_defeated=assets/raw/sp_baraka_defeated.png
```

Скрипт пишет файлы как `<id>.webp` (`sp_baraka_idle.webp`), а в манифесте они `baraka_<pose>.webp` — переименовать вручную (`gen-assets.ts` делает это сам, а при прямом вызове нет). Крупный тёмный персонаж с текстурной кожей сжимается хуже Милины — проверить все четыре позы.

```bash
git add -A && git commit -m "assets(rank1): meeting room background, Baraka portraits and sprites"
```

---

### Task 4: Прогон в игре

- [ ] **Step 1:** `npm run dev`. Пройти ступень 0 (победить Милину), убедиться, что во время событий ступени 0 в фоне ушёл `prefetchGroup('rank1')` (вкладка Network: `meeting_room.webp`, `baraka_*.webp`). Ступень 1: оба события с портретами Бараки на фоне переговорки; реакция с портретом angry на втором варианте события «KPI»; в событии «Конфликт в чате» третий вариант («Пошутить») виден только после шутки на онбординге ступени 0, четвёртый («Чеклист») — только после первого варианта «KPI». Проверить HUD: должность «Младший специалист Внешнего Мира».
- [ ] **Step 2:** Бой: Барака слева-направо смотрит на героя, ноутбук с красным экраном в руке, позы меняются без прыжков, «Согласиться» — двойной урон, «Пошутить» — серая цифра и реплика из `immune`. Спецприём с репликой капсом по центру и `dim`. Победа за 3–5 ходов при слабости (терпение 110), поражение — стресс +25, повтор с `repeatText` события «KPI». После победы — ступень 2 (пока dev-финал `dev_shao`, это ожидаемо до этапа 5).
- [ ] **Step 3:** Скриншоты `docs/screenshots/stage4-event.png`, `docs/screenshots/stage4-battle.png`. README: статус «этап 4 из 8», потраченная сумма.

```bash
git add -A && git commit -m "docs: stage 4 status"
```

## Что дальше

Этап 5 (`2026-09-03-05-rank2-kitana.md`): ступень 2 «Специалист», Китана и переговоры об альянсе, кухня с кулером; флаг `baraka_respect` открывает там дополнительный вариант.
