# Планы реализации Corporate Mortal Kombat

Спека: `../specs/2026-09-03-corporate-mortal-kombat-design.md`. Планы выполняются строго по порядку, каждый даёт работающую игру.

| # | План | Результат |
|---|------|-----------|
| 1 | `2026-09-03-01-core.md` | Движок, бой на прямоугольниках, VN, звук, три концовки, тестовый босс |
| 2 | `2026-09-03-02-assets-pipeline.md` | Генерация ассетов через OpenRouter, постобработка, загрузчик; герой, титул, концовки, музыка, диктор |
| 3 | `2026-09-03-03-rank0-mileena.md` | Ступень 0 «Стажёр», Милина |
| 4 | `2026-09-03-04-rank1-baraka.md` | Ступень 1 «Младший специалист», Барака |
| 5 | `2026-09-03-05-rank2-kitana.md` | Ступень 2 «Специалист», Китана |
| 6 | `2026-09-03-06-rank3-sindel.md` | Ступень 3 «Руководитель направления», Синдел |
| 7 | `2026-09-03-07-rank4-shang-tsung.md` | Ступень 4 «Директор департамента», Шанг Цунг и Рептилия |
| 8 | `2026-09-03-08-final-shao-kahn.md` | Финал: Шао Кан, эпилоги, симуляция баланса, деплой |

Флаги, которые ставят ступени (используются позже как `requiresFlag`): `intern_joker` (0), `baraka_respect` (1), `kitana_owes_you` (2), `sindel_budget_ally` (3), `shang_secret` (4).

Каждый план прошёл архитектурное ревью; замечания вшиты в текст планов.

## Этап 1 выполнен — заметки для этапа 2

Перенесено из ledger финального ревью этапа 1 (всё остальное закрыто):

- `assets/manifest.json` импортируется в браузерный бандл целиком; при появлении промптов нужен runtime-срез (только `id, kind, group, file, size, anchor, duration`).
- Музыка: fade-out `setTargetAtTime(0, t, 0.4)` при `stop(t + 1.2)` оставляет ~5% амплитуды (щелчок); старые source-узлы не отключаются. Проверить на первом реальном `mu_*`.
- Титул: `playMusic('mu_title')` вызывается по клику, а не при монтировании — после рестарта музыка концовки продолжает играть на титуле; звук разблокируется по кнопке «Начать», кнопки mute на титуле нет.
- Контракт `AssetStore` описан в `src/assets.ts` — реальный загрузчик обязан его соблюдать (`loadGroup` не реджектится, прогресс монотонный до 1, `getTexture` никогда не null, `getImageUrl` — null или URL, безопасный в `url('…')`).
- Проверить кадрирование камеры при zoom 1.4 на реальных спрайтах с якорем в ногах (pivot в центре экрана).
- Полировка: пропуск печатающегося текста только мышью; `aria-label` кнопки mute не отражает состояние; `innerHTML` с контентными строками; favicon отсутствует.

## Этап 2 выполнен — заметки для этапов 3+

- Сгенерирован только герой (2 портрета + 4 спрайта, $0.34); финальный art-проход по титулу, концовкам, музыке и диктору отложен до утверждения сюжета.
- Финальная волна фиксов этапа 2 выполнена. Осталось отложенным (не блокирует): runtime-срез манифеста для бандла (промпты ≈ 9 КБ, пересмотреть после 50 КБ); pcm16-fallback в `tools/openrouter.ts` жёстко считает 24 кГц моно (только OpenAI); у аудио-записей нет raw, поэтому `--force` на музыке/голосе всегда тратит деньги; размер холста 700×900 зашит в `gen-assets.ts`; нет `../`-защиты в `refToDataUrl`; нет unit-тестов контракта `AssetStore`; в `check-assets` статус `deferred` определяется по пустому промпту.
- Известная тонкость автоключа: если фигура заметно (>~1 %) заходит на рамку, диагностика скажет «degenerate chroma key», а не «touches the frame border» — в обоих случаях смотреть raw и переснимать позу.
- Для планов 03–08 контентные задачи (события, диалоги, баланс) выполняются на заглушках как обычно; задачи генерации боссов пропускаются до общего art-прохода.
- Сгенерировать одного персонажа отдельно позже: `npm run gen -- --only id1,id2` (см. `README.md`, раздел «Ассеты»).
- Хромакей у `tools/postprocess.py` автооценивает цвет фона по рамке картинки, так что неидеальная маджента — не проблема; важно, чтобы фигура не касалась края кадра.

## Этап 3 выполнен — заметки для этапов 4+

- Сделаны задачи 1, 2 и 4 (на заглушках); задача 3 (генерация Милины и опенспейса, 7 записей `group: rank0`, `generated: false`) отложена до общего art-прохода. Промпты уже в манифесте, `npm run gen -- --dry-run --group rank0` показывает 9 записей (с `mu_office`, `mu_battle`) ≈ $0.48.
- Движок показывает только `lines.defeated[0]` (`choreo.ts`) — вторая реплика в `defeated` была бы мёртвым текстом; у боссов держать одну. ~~`lines.hit` показываются только при ударе героя…~~ **Устарело (2026-09-04):** `intro`, `lines.hit` и `lines.immune` заменены обменами `Boss.exchanges` — см. раздел «Бой как диалог» ниже.
- ~~Пузырь `intro` видно ~2 с до `FIGHT!`~~ **Устарело:** пузырь теперь висит до хода игрока (лимиты — в валидаторе, см. «Бой как диалог»). Реплики `special` идут крупным центрированным текстом ~1.4 с — до ~60 знаков (в силе).
- Тест `test/content.test.ts` ищет намёки на слабость и иммунитет только в `text` и `repeatText` последнего события (не в вариантах и реакциях): игрок видит одну реакцию из нескольких.
- Герой — мужчина. Прежнее правило «герой без пола» (2026-09-03…04) отменено 2026-09-05 по решению автора; мужские формы прошедшего времени к герою допустимы.
- Сквозные шутки, заведённые на ступени 0 (подхватывать, не повторять дословно): черепа как «инвентарь» и «полка» бывших сотрудников; кофе-машина сломана со времён Первой Династии, «заявка в работе»; слайд 47, которого не существует; Барака считает кивки — «у него по ним KPI» (задел на слабость `agree` ступени 1); Барака уже спрашивает в чате, кто вы (задел на «конфликт в чате» ступени 1); Китана — дочь генерального, ей «можно» (задел на ступень 2); регламент кофе-машины лежит в серверной-подземелье у Шанг Цунга, «за ним спускались трое» (задел на ступень 4). Милина — на «вы», Барака по плану 04 — на «ты».
- Флаг `intern_joker` ставится третьим вариантом первого события ступени 0.
- Баланс: на ступени 0 при игре по слабости герой поглощает 34–51 урона из 100 — ниже целевой полосы спеки 60–90; для первого босса это нормально, но учесть в симуляции этапа 8.
- Intro dev-финала (и будущее intro Шао Кана, план 08) сокращено до 78 знаков: «Пять минут. Меня не интересуют цифры. Меня интересует, кто со мной согласен.» — план 08 цитирует старую длинную формулировку, брать эту.
- `stylePrefix` говорит «shallow depth of field»; для фонов-планов в промпте явно переопределять глубину резкости (сделано в `bg_openspace`).
- Движок больше не зеркалит спрайт босса (`scene.ts`, было `new Fighter(BOSS_X, true, …)` со времён заглушек): боссы по спеке рисуются влево, поэтому промпты «facing LEFT» с `flip: false` верны; иначе после генерации босс смотрел бы от героя. Заглушки-прямоугольники симметричны, разница видна только на реальном арте.

## Этап 4 выполнен — заметки для этапов 5+

- Сделаны задачи 1, 2 и 4 (на заглушках); задача 3 (генерация Бараки и переговорки, 7 записей `group: rank1`, `generated: false`) отложена до общего art-прохода. `npm run gen -- --dry-run --group rank1` ≈ $0.40.
- Флаг `baraka_respect` ставится первым вариантом события «KPI» ступени 1 («Согласиться: цифры честные…»). План 05 открывает им дополнительный вариант у Китаны.
- Голоса: рассказчик в тексте событий обращается к герою на «вы» на всех ступенях; Барака в своих репликах — на «ты».
- Формула повтора у каждого босса своя: Милина — «Второй заход. Напоминаю…», Барака — «Итерация вторая. Записываю в протокол…». Китане и дальше — не копировать.
- Внутри реплики вложенные кавычки — „лапки“ («…ответ — „согласен“…»), не вторые «ёлочки».
- Новые сквозные шутки ступени 1: сорок семь — число Бараки (слайд 47 существует, он его сделал; 47 % задач; 47 файлов в пул-реквесте); колодец для брейнштормов в переговорке «Кровавый колодец» — идеи туда падают и не возвращаются; Рептилия сослан в поддержку за шутку на ревью; лайк — половина кивка «по регламенту»; линейки-клинки меряют отступы; Китана снова ставит смайлик шутке героя; «убери из кода слово „синергия“».
- Баланс: по слабости `agree` герой побеждает за 3–4 хода и поглощает 26–46 урона (ниже полосы 60–90 спеки, как и на ступени 0); путь `data` — 7 ходов и ≈ 81 урона; `joke` проигрывает. Учесть в симуляции этапа 8.
- Реплика поражения Бараки — 70 знаков: пузыри боя не печатаются посимвольно и начинают гаснуть через ~1.6 с (`effects.ts`), при 360 px ширины больше ~75 знаков не прочитать.


## Этап 5 выполнен — заметки для этапов 6+

- Сделаны задачи 1, 2 и 4 (на заглушках); задача 3 (генерация Китаны и кухни, 7 записей `group: rank2`, `generated: false`) отложена до общего art-прохода. `npm run gen -- --dry-run --group rank2` ≈ $0.40.
- Флаг `kitana_owes_you` ставится третьим вариантом события «Выбор стороны» (шёпот про график с правками Шанг Цунга) и тратится в «Корпоративе» на её слайды; дальше этот флаг не нужен — Китана сказала «мы в расчёте». `baraka_respect` открывает четвёртый вариант «Выбора стороны». Для `intern_joker` на ступени 2 слота не нашлось (оба события уже по 4 варианта) — при желании подхватить на ступени 3.
- Голоса: Китана — на «вы», договорной регистр («пункт первый», «вторая редакция», «протокол разногласий», «ратифицируют», «приложение Б», «мы в расчёте»), без восклицаний вне `special`. Формулы повтора заняты: Милина «Второй заход. Напоминаю…», Барака «Итерация вторая. Записываю в протокол…», Китана «Вторая редакция…» / «Протокол разногласий. Пункт первый…». Синдел — своя.
- `repeatText` не должен утверждать, какой вариант игрок выбирал в прошлый раз (ревью этапа 5 поймало «вы принесли настроение» на ветке, где игрок собрал отчёт): единственный факт, верный на повторе, — проигранный бой.
- Тон: шутки про Милину — про HR-поведение и караоке, не про то, что под вуалью (ревью сняло «из милосердия»).
- Новые сквозные шутки ступени 2: записки Шанг Цунга на холодильнике («НЕ ТРОГАТЬ. Ш. Ц.» → «Я ЗНАЮ, КТО ТРОГАЛ. Ш. Ц.», «йогурт был мой»); веера из слайдов; «согласных у меня этаж, вакансий на нём нет»; «настроение не ратифицируют»; слайды третий и двенадцатый — «для отца» (Шао Кан назван только так, задел на этап 8); «трое спустились… и стали эффективнее навсегда»; Барака требует убрать «синергию» из кода, Китана — из тостов; реплика поражения «И не спускайтесь в серверную» — задел на Шанг Цунга (ступень 4).
- Баланс (формулы `battle.ts`): по слабости `data` при компетентности 40 — 5 ходов, поглощено ≈58; при 50–60 — 4 хода, ≈45; нейтральный `blame` — 10 ходов, проигрыш; `agree` (иммунитет) безнадёжен. Плейтест: победа за 5 ходов (4 data + 1 agree). Ближе к полосе 60–90, чем ступени 0–1, но всё ещё ниже — учесть на этапе 8.
- Плейтест через Playwright: `el.click()` на кнопке `.result-next` не срабатывает, `dispatchEvent(new MouseEvent('click', {bubbles:true}))` — да; текст пузырей боя живёт в canvas, из DOM не читается.
- Нюанс манифеста: у Китаны палитра принудительно синяя/серебряная (вуаль — royal blue, иначе уйдёт в прозрачность при мадженте); план Б с зелёным ключом описан в плане 05, задача 3.

## Этап 6 выполнен — заметки для этапов 7+

- Сделаны задачи 1, 2 и 4 (на заглушках); задача 3 (генерация Синдел и зала совета, 7 записей `group: rank3`, `generated: false`) отложена до общего art-прохода. `npm run gen -- --dry-run --group rank3` ≈ $0.44 (8 записей вместе с ещё не сгенерированным `mu_council`).
- Флаг `sindel_budget_ally` ставится третьим вариантом «Бюджета» (резервный фонд под контролем Синдел) и пока никем не читается — слот для ступени 4 (Шанг Цунг): союзник-финдиректор может открыть вариант в «Утечке» или «Интриге». `kitana_owes_you` использован в «Бюджете» не как долг (он закрыт на ступени 2: «мы в расчёте»), а как знание из шёпота — график Китаны в серверной с правками Шанг Цунга. `baraka_respect` и `intern_joker` открывают третий и четвёртый варианты «Найма» — `intern_joker` впервые сыграл после ступени 1.
- Голос Синдел: на «вы», бухгалтерско-королевский регистр (строка, статья, амортизация, гроссбух, красная ручка, «согласовано»), кричит только в `special`. Формула повтора — «Пересчёт.» / «Сверка.»; заняты также: Милина «Второй заход…», Барака «Итерация вторая…», Китана «Вторая редакция…» / «Протокол разногласий…». Шанг Цунг — своя.
- Правило повтора уточнено ревью этапа 6: `repeatText` не должен утверждать и то, какие приёмы игрок использовал в проигранном бою (было «нет имени виноватого» при слабости `blame`).
- Реплики `hit` звучат на любой не-иммунный приём — формулировки не должны предполагать конкретный ход (ревью сняло «Дочь бы поставила смайлик» как реакцию на шутку и слайд 47 как реакцию на данные).
- Семейная фирма: Синдел — мать Китаны и жена генерального («скажу ей я, дома»); генеральный по-прежнему не назван по имени — задел на этап 8. Серверная — единственная статья, которую Синдел не смогла проверить («В серверную не ходите, я пробовала») — задел на ступень 4.
- Новые сквозные шутки ступени 3: «расход без виноватого — дыра, расход с виноватым — статья»; «смех не амортизируется»; закладка в гроссбухе на фамилии героя (на повторе красная); строку «ремонт кофе-машины» Синдел вычёркивает с Первой Династии — вот почему заявка «в работе»; резюме «умею» племянника Бараки, «зубы, амортизация»; чувство юмора на балансе направления, списанию не подлежит; «Милину сюда не пускают»; слайд 47 существует — у Синдел чек; черепа с инвентарными номерами.
- Баланс: `blame` при репутации 30 → 29 урона, 6 ходов, 5 ответов Синдел ≈ 79 поглощено (в полосе 60–90); проигрыш только при 5 спецприёмах подряд (~0.1 %). При репутации 20 (игрок ни разу не брал репутацию) — 7 ходов и ≈ 17 % проигрышей. Ревью отметило: без флагов ступень 3 сама почти не кормит репутацию (+5 максимум) — учесть в симуляции этапа 8. Атака оставлена 13 по спеке.
- Плейтест (Playwright, заглушки): оба события ступени 3 показывают по 4 варианта при всех флагах; Синдел побеждена за 6 ходов `blame` при репутации 30; после неё идёт dev-финал с «Ударить»; сетевых запросов к `generated: false` ассетам нет.

## Этап 7 выполнен — заметки для этапа 8

- Сделаны задачи 1, 2 и 4 (на заглушках); задача 3 (генерация Шанг Цунга, Рептилии и серверной, 8 записей `group: rank4`, `generated: false`) отложена до общего art-прохода. `npm run gen -- --dry-run --group rank4` ≈ $0.45.
- Флаг `shang_secret` ставится вторым вариантом «Интриги» (сделать вид, что согласны, и включить запись: общий компромат героя и Шанг Цунга). **У флага пока нет потребителя:** план 08 его не упоминает, а финальная ступень по спеке без событий, так что вариантом его не прочитать. Этапу 8 решить: либо ветвить текст концовки/intro Шао Кана по `state.flags` (правка движка), либо убрать флаг из плана. Остальные флаги на ступени 4: `kitana_owes_you` — третий вариант «Утечки» (как знание о графике в серверной, не как долг), `intern_joker` — четвёртый вариант «Утечки», `sindel_budget_ally` — третий вариант «Интриги», `baraka_respect` — четвёртый.
- Голоса: Рептилия — техподдержка (тикет, приоритет, лог, «не воспроизводится»), шипит на «с» изредка; Шанг Цунг — на «вы», бархатный консультант-колдун (трансформация, дорожная карта, лица и голоса, планшет-артефакт), единственный, кто любит слово «синергия», не кричит вовсе: его `special` — шёпот по центру. Формула повтора «Дубль второй.»; заняты также: Милина «Второй заход…», Барака «Итерация вторая…», Китана «Вторая редакция…» / «Протокол разногласий…», Синдел «Пересчёт.» / «Сверка.», Рептилия «Тикет переоткрыт.». Шао Кану — своя.
- Генеральный по-прежнему не назван по имени: реплика поражения Шанг Цунга «Продолжим в тронном кабинете» — прямой мост к этапу 8. Заделы для финала: Шанг Цунг «на прошлом ревью полтора часа был Баракой» (перевёртыш может появиться в тронном кабинете чужим лицом); коробка с чужими пропусками в серверной, отчёт открыт пропуском героя; полка обвинений; кофе-машина в сети с Первой Династии (регламент починки до сих пор у Шанг Цунга — так и не починена; можно закрыть шутку в концовке); «у меня на данные аллергия, они не трансформируются» — Шао Кан по спеке тоже иммунен к `data`, различить регистр.
- Новые сквозные шутки ступени 4: тикет 6660; «меня двенадцать лет никто не замечает, это моя основная компетенция»; «тихо в серверной не бывает, тут всё пишется в лог»; «не воспроизводится — самая популярная резолюция в компании»; «продакшен под напряжением»; общий секрет ценнее подписи («подпись можно оспорить»); «Трое приходили сюда с тем же аргументом. Двое остались».
- Правила текста, подтверждённые ревью этапа 7: тексты событий держать до ~550 знаков и реакции до ~240 (тайпрайтер 22 мс/знак — 627 знаков это ~14 с до вариантов); намёки на слабость/иммунитет вплетать в одну-две фразы, а не перечислять четырьмя подряд; `repeatText` не должен утверждать «трижды не предупреждаю» и подобное, ложное на третьем повторе; для спрайтов с полупрозрачными эффектами (свечение, дым) в промпт добавлять «solid opaque shapes, no translucent elements» — мягкая кромка хромакея даёт смеси частичную альфу.
- Баланс (формулы `battle.ts`, симуляция 20 000 боёв): по слабости `data` при компетентности 40–59 — 6 ходов, поглощено 76–93, проигрыш 0–10 % в зависимости от стресса; при компетентности ≥ 60 — 5 ходов, поглощено 61–74, без проигрышей; при компетентности 30 (игрок ни разу не брал компетентность) — 7 ходов и 17–82 % проигрышей. Ступень сама даёт компетентность +10 в первом варианте «Утечки» и во втором «Интриги». Атака оставлена 13 по спеке. Плейтест: вход на ступень L70 R40 C50 S0 (путь по флагам), Шанг Цунг побеждён за 5 ходов `data` (≈19 × 2).
- Плейтест (Playwright, заглушки): оба события ступени 4 показывают по 4 варианта при всех флагах; после Шанг Цунга идёт dev-финал с «Ударить»; консоль — только favicon 404; сетевых запросов к `generated: false` ассетам нет (включая `endings`: префетч на ступени 4 срабатывает, но записи не сгенерированы, так что проверка из плана «в Network видно il_ending_*» на заглушках ненаблюдаема).

## Этап 8 выполнен — заметки для art-прохода и поддержки

- Сделаны задачи 1, 2, 3, 5, 6, 7 (на заглушках); задача 4 (генерация Шао Кана, тронного кабинета и Джакса, 8 записей `rank5`/`endings`) отложена до общего art-прохода. Сюжет завершён: шесть ступеней, три концовки, у повышения два варианта.
- **Флаг `shang_secret` получил потребителя**: `Ending.variants` (`src/types.ts`) — список `{ requiresFlag, title?, text?, epilogue? }`; `resolveEnding(ending, flags)` в `state.ts` берёт первый вариант с установленным флагом и перекрывает поля базовой концовки. Вариант повышения «Заместитель Шао Кана по трансформации» с эпилогом Шанг Цунга (`pt_shang_neutral`, группа `rank4` — к концовке уже загружена). Валидатор проверяет портреты эпилогов, ключ ↔ `id` и что флаг варианта где-то ставится.
- Эпилоги (`Ending.epilogue`): Шао Кан (повышение), герой `pt_hero_worried` (выгорание), Джакс `pt_jax_neutral` (Fatality). Экран концовки ждёт `loadGroup('endings')`, весь текст через `textContent`, панель `overflow-y: auto`.
- Баланс: `tools/simulate.ts` (`npx tsx tools/simulate.ts 1000`) и `test/balance.test.ts`. Шао Кан **140/14** с 2026-09-04 (этап 8 ставил 190/15, спека изначально 190/14 — см. ниже). Плановое условие «два последних босса ≤ 95 % с первого раза» недостижимо при стрессе 0 (4–5 ответов × 1.125 × атака < 100) и заменено окном поглощённого урона 60–90 по формуле спеки. Варианты с шестью ходами (терпение ≥ 210) дают спираль: при стрессе 40 ≈ 96 поглощённых, после проигрыша (стресс 65) ≈ 97, второй проигрыш — Выгорание; плейтест это подтвердил (220/15 — два поражения подряд). При 190/15: лояльность ≥ 65 — проигрыш ≤ 0.4 % при любом стрессе ≤ 65; лояльность 55 — 37 % при стрессе 40. **Смягчение 2026-09-04:** игрок с плохими статами (сценарий `events=worst`: лояльность ≈30, стресс растёт) при 190/15 проигрывал финал в 100 % случаев даже по слабости и попадал в спираль (+25 стресса за поражение). При 160/14: worst 89 %, random 98 %, best 100 % за 4 хода. После добавления двух событий перед финалом худший путь стал на 5 лояльности беднее и на 15 стресса богаче, worst упал до 66 % — терпение опущено до 140 (порог: пять «Согласиться» худшего пути ≈ 140; 145 уже даёт 66 %). При 140/14: worst 100 % за 5 ходов, random 99 %, best 100 % за 4 хода; случайные приёмы 0 %, Fatality 99 %. Окно поглощённого урона в тесте опущено до 45–90, добавлен тест «худшие события ≥ 80 % / промоушен ≥ 85 %». Симулятор получил стратегию событий `worst` и сценарии «слабость при random/first/worst событиях».
- FINISH HIM (терпение < 15 %) при сильном «Согласиться» (лояльность 95, урон 48) не появляется: 190 → 46 → победа. Fatality — концовка «намеренно ослабленных ходов» (три «Согласиться», затем «Пошутить» до открытия карточки); тултип заблокированной карточки теперь об этом говорит. Если захочется сделать Fatality доступнее — менять порог в `battle.ts` (спека: 15 %) или терпение.
- Голос Шао Кана: «ты», имперское презрение, цифры не аргумент, потому что реальность подчиняется («график растёт туда, куда я скажу»); крик капсом только в `special`. Формула повтора теперь нужна: у финала два события (см. ниже). Закрыты шутки: кофе-машина («заявка всё ещё в работе, с Первой Династии»), «синергия» под запретом, Синдел тридцать лет носила данные, Шанг Цунг говорил голосом императора, шлем как инвентарь (Джакс), тикет 6660 переоткрыт в третий раз.
- Плейтест (Playwright, заглушки): три полных прохождения — повышение с флагом (17 недель, финал за 5 ходов, «Ударить» открылось на 5-м), повышение без флага (лояльность 95, 4 хода, FINISH HIM не показан), Fatality (6 ходов), Выгорание (три поражения Милине «стрелками»). Консоль — только favicon (добавлен пустой `<link rel="icon">`), сетевых запросов к `generated: false` нет. Сборка `dist/` проверена под подпутём `/corporate-mortal-combat/dist/` через `python3 -m http.server` из родительской папки.
- Деплой: `.github/workflows/pages.yml` (node 24, `npm ci --ignore-scripts`, тесты, сборка, `actions/deploy-pages@v4`). Страница ждёт арт: до art-прохода на GitHub Pages видны заглушки-прямоугольники с id.
- Art-проход (когда сюжет утверждён): `npm run gen -- --dry-run` показывает 48 картинок ≈ $2.91 и 14 аудио ≈ $0.40; порядок — по группам `rank0`…`rank5`, `endings`, `core` (титул), потом звук; после каждого персонажа смотреть спрайты (плащ/свечение внутри кадра, одинаковый масштаб, взгляд влево) и `npm run assets:check`.

## Art-проход выполнен — заметки для поддержки

- Все 68 записей манифеста сгенерированы (2026-09-04): `npm run assets:check` — generated 68, missing 0, over budget 0, 5.0 MB. Сессия стоила ≈ $3.45 (rank0 0.67 с пересъёмкой Милины, rank1 0.49, rank2 0.54, rank3 0.43, rank4 0.38, rank5 0.36, endings/core/голоса 0.55, бисект модерации 0.09); всего по проекту ≈ $3.9.
- **Зелёный ключ** для Милины, Синдел и Шао Кана (`chroma: "#00FF00"`, в промптах убраны все упоминания мадженты): на мадженте бордовый костюм и фиолетовая вуаль уходили в прозрачность. Правило: цвет ключа выбирать по контрасту с палитрой персонажа, а не фиксированно.
- Во все промпты спрайтов добавлены четыре фразы: плоский равномерный chroma-key экран без виньетки/градиента; «FULL-LENGTH FULL-BODY SHOT» в начале; «whole figure, including both feet, fully inside the frame with a clear margin»; фигура ≈ 85 % высоты кадра. Без них Seedream рисует студийный фон с тенями и режет по колено/по ступни (Милина hurt, Барака idle/attack, Китана idle/attack/hurt, Синдел attack — все пересняты).
- Позы с предметами у края кадра ломаются: веера Китаны в defeated «сложены и прижаты к груди», у Бараки в attack «exactly two solid opaque steel ruler-blades, no ghosted or translucent blades» (первая версия дала лишние полупрозрачные клинки), у Синдел attack — компактные волосы. Пересъёмка по замечанию пользователя: `sp_mileena_attack` (три ноги).
- `tools/postprocess.py`: `near = min(p99(рамки)+8, 30)` — без потолка фигура, касающаяся рамки, раздувает порог; тест в `postprocess_test.py` проверяет сообщение `touches the frame border`.
- **Модерация Seedream**: блок = 400 и $0, результат стохастичен (`pt_shao_angry` прошёл с третьей попытки без изменений). Бисект (`bisect.ts` в scratchpad сессии, варианты noref/noname/nohelmet/minimal) показал: триггер — постер `post-hero-and-shao-kahn.png` как референс в связке с описанием Шао Кана, а не слова. Итог: `sp_shao_idle` без референсов, остальные позы — по `sp_shao_idle`, `il_title` — по `pt_hero_neutral` + `pt_shao_neutral` (dependsOn), `pt_shao_*` по постеру. Принятое расхождение: у спрайтов Шао Кана шлем — полная костяная маска, у портретов — полумаска.
- **Диктор**: `AssetEntry.system` (`src/types.ts`) → `AudioRequest.system` (`tools/openrouter.ts`) → system-сообщение чата. Без него `gpt-audio-mini` отказывался («I can't…»), читал ремарки вслух или добавлял слова; файлы выходили 48–142 KB против бюджета 40 KB. `vo_title` и `vo_flawless` («Lawless») пересняты до точного транскрипта.
- Принятые мелкие дефекты (не переснимать без причины): нога Милины в defeated у края кадра; Барака attack мельче других поз; голоса — через chat-audio модель, не TTS.
- Процесс: длинные `npm run gen` запускать в foreground (фоновые задачи дважды обрывались на шаге кропа; перезапуск `--group` без `--force` докручивает постобработку по готовому сырью); ключ из `.env` не печатать — вывод пропускать через `sed 's/sk-or-[A-Za-z0-9-]*/[KEY]/g'`.
- Плейтест с артом (Playwright, 1280×720): титул, событие ступени 0 и бой с Милиной — герой и босс в одном масштабе, ступни на одной линии, лицом друг к другу; консоль без ошибок. Скриншоты в `docs/screenshots/art-*.png`.

## Бой как диалог выполнен (2026-09-04) — заметки для поддержки

Спека: `../specs/2026-09-04-battle-dialogue-design.md` (дополняет разделы «Боевая механика» и «Хореография боя» основной спеки; при противоречии главнее она).

- Модель: `Boss.exchanges: Exchange[]` — ровно `EXCHANGE_COUNT = 8` обменов (`battle.ts`), каждый — `prompt` босса и `replies` по четырём тактикам `{ text, reaction }`. Порядок 0→7, затем по кругу последние `LOOP_TAIL = 3`. `BattleState.exchange` — текущий обмен; `TurnResult` без текстов (реакцию и следующую реплику `choreo.ts` берёт через `exchangeAt(boss, i)` по `prev.exchange` и `r.battle.exchange`). `intro`, `lines.hit`, `lines.immune` удалены; `strikeText` у финального босса — текст пятой карточки.
- `rng` за ход вызывается ровно два раза и всегда (спецприём, индекс `special`), раньше — три; поток seed 42 сдвинулся, симуляция перезапущена: путь по слабостям — повышение 98 %, все боссы за 4–7 ходов, `test/balance.test.ts` зелёный без перенастройки.
- Валидатор (`schema.ts`, `TEXT_LIMITS`): реплика ≤ 110, ответ ≤ 80, реакция ≤ 75, `strikeText` ≤ 60, ключи ответов ровно `agree, data, blame, joke`, глобальная уникальность всех текстов боя по всем боссам. Лимиты выведены из геометрии пузыря (ширина переноса 380, низ на 300, верх не выше 100) и карточек (пять колонок на 1280 px ≈ 30 знаков в строке, три строки) — не поднимать без пересчёта.
- Подача (`effects.ts`): контейнер с меткой `speech` между бойцами слева от босса, растёт вниз от HUD; `append` добавляет второй пузырь под первым (реакция приглушается, хвостик к боссу переходит на новый вопрос) — с 2026-09-05, раньше текст дописывался абзацем в тот же пузырь; при столбике выше 500 px шрифт 20→17. Новый пузырь без `append` уничтожает старый мгновенно; `clearSpeech` (контроллер, в момент хода) гасит за 0.25 с. Таймер 99 стоит на `awaitInput` (`scene.pauseTimer/resumeTimer`) и идёт только во время анимаций. Карточки строятся через `createElement`/`textContent`, скрыты во время интро (`.moves[hidden]`).
- Правила текста для новых обменов: четыре ответа узнаются как тактики и без подписи; реакция на слабость — босс задет, на иммунитет — отмахнулся, нейтральная — принял к сведению; герой ко всем боссам на «вы»; обмены 5–7 без ссылок на порядок («последний вопрос» нельзя); у Шао Кана `exchanges[0].prompt` обязан содержать слова-намёки на `agree` и `data` (`HINT_WORDS` в `test/content.test.ts`). Скрипт-проверка черновиков (лимиты, дубли) жил в scratchpad сессии; в репозитории те же проверки делает валидатор.
- Реплики `special` и `defeated` не менялись; `hit`/`immune` частично перенесены в реакции.
- Плейтест (Playwright, 1280×720): полное прохождение до концовки «Повышение» автодрайвером по слабостям, консоль без ошибок; скриншоты `docs/screenshots/art-battle-dialogue.png` и `art-battle-shao-kahn.png`. Драйвер кликает карточку по подписи `.move-meta` (название тактики), кнопку `.result-next` — через `dispatchEvent(new MouseEvent('click', { bubbles: true }))`.


## Прелюдия финала (2026-09-04)

- Пользователь: «почему в тронном зале сразу бой, без прелюдий». У ступени 5 появились два события, как у остальных: `deputy_night` («Ночь перед пятиминуткой», Милина из HR как спикер, в реакциях Синдел, Китана и Барака) и `deputy_reception` («Приёмная», Джакс, начальник охраны, `pt_jax_neutral`). Новых ассетов нет: фон `bg_throne_office` и все портреты уже были.
- Валидатор больше не требует у финала нуля событий: правило одно — ровно два события у каждой ступени (`schema.ts`, тест «последняя ступень без 2 событий — ошибка»). Фикстура `makeRanks` даёт финалу два события с id `f_ev1`/`f_ev2`; путь движка «ступень без событий → сразу бой» сохранён и проверен явной фикстурой в `test/state.test.ts`.
- Намёк на слабость/иммунитет Шао Кана теперь даёт текст «Приёмной» («император любит согласных», «распечатки с цифрами сдаём») — `test/content.test.ts` берёт последнее событие; запасной путь через `exchanges[0].prompt` оставлен.
- Потребители флагов в прелюдии: `kitana_owes_you` и `baraka_respect` (ночь), `shang_secret` и `intern_joker` (приёмная); `sindel_budget_ally` в финале не используется. Джакс в «Приёмной» не говорит «сэр», «камер в кабинете нет» — задел на карточку «Ударить» и его же эпилог после Fatality.
- Эффекты выборов намеренно мягкие (худший путь: лояльность −5, стресс +15): прелюдия перед финалом не должна быть ловушкой; из-за неё же терпение Шао Кана опущено до 140 (см. «Баланс»).

## Партнёрство без боя и перемешивание обменов (2026-09-04)

- Пользователь: «развилка у Шао Кана: не побеждаем, а становимся партнёрами, если пришли с хорошими рекомендациями; бой можно избежать». Порог согласован: **две из трёх** рекомендаций (`baraka_respect`, `kitana_owes_you`, `sindel_budget_ally`), иллюстрация — титульный арт `il_title` (рукопожатие), музыка — новый трек `mu_partnership` (Lyria, ≈ $0.04, группа `endings`).
- Механика: `Choice.requiresFlags: { flags, min }` (виден при ≥ min флагов), `Choice.ending: EndingId` (выбор завершает игру), `GameState.ending` (назначенная концовка, `checkEnding` отдаёт её первой), `Ending.music`. Пятый вариант «Приёмной» ведёт в `partnership`; валидатор допускает до 5 вариантов, проверяет флаги `requiresFlags` и существование концовки. Экран концовки грузит ещё группу `core` (ради `il_title`) и берёт `ending.music ?? 'mu_ending'`.
- Симулятор: стратегия событий `allies` (берёт выборы с флагами и развилку) → партнёрство 100 %; `best` по очкам статов рекомендации не собирает (0 %) — развилка награда за союзников, не за максимум статов; при случайных событиях 6 %. Тест в `test/balance.test.ts`.
- Перемешивание: `src/exchanges.ts`, `orderExchanges(boss, seed)`; сид партии в `GameState.seed` (createInitialState), реванш идёт в том же порядке. Первый обмен фиксирован (намёк на слабость), тасуются 1–4 и хвост 5–7 отдельно; `Boss.fixedExchanges` — у Китаны 5 (нумерованные пункты договора). Реплики проверены на зависимость от порядка: «Дубль второй» и «пункт первый» только в фиксированных позициях.
- Расход: генерация `mu_partnership` ≈ $0.04, итого по проекту ≈ $3.94 из $10.
